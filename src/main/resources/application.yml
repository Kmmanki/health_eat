server:
  address: localhost
  port: 8080
spring:
#  datasource:
#    driver-class-name: com.mysql.cj.jdbc.Driver
#    password: tlawjddn1!
#    url: jdbc:mysql://localhost:3306/healtheat?useSSL=false&useUnicode=true&serverTimezone=Asia/Seoul&allowPublicKeyRetrieval=true&characterEncoding=utf8
#    username: healtheat
  h2:
    console:
      enabled: true
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL5InnoDBDialect
    show_sql: true
    hibernate:
      ddl-auto: update